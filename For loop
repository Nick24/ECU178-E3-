for t in range(1, 500):
    X1,Y1,X2,Y2=canvas.coords(id2)
    if X1 >= x_max:
        vx = -10.0
    if Y1 <= y_min:
        vy = 5.0
    if Y2 >= y_max:
        vy = -5.0
    if X1 <= x_min:
        vx = 10.0
    canvas.coords(id2,X1+vx,Y1+vy,X2+vx,Y2+vy)
    canvas.update()
    time.sleep(0.1)
